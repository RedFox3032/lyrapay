import 'package:flutter/material.dart';

class TransactionReceiptSheet extends StatelessWidget {
  const TransactionReceiptSheet({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
