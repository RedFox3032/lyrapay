import 'package:flutter/material.dart';

class VoucherInputWidget extends StatelessWidget {
  const VoucherInputWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
