import 'package:flutter/material.dart';

class PinPadWidget extends StatelessWidget {
  const PinPadWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
