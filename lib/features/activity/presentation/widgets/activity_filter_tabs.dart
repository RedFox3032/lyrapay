import 'package:flutter/material.dart';

class ActivityFilterTabs extends StatelessWidget {
  const ActivityFilterTabs({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
